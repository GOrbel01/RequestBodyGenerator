package org.fsq.requestbodygenerator.data;

public interface Constants {
    /**
     * Assume 'A a;' as smallest possible (even if for now Complex types not supported)
     */
    Integer MIN_LENGTH = 4;
}
